<%= form_for(@invitation) do |f| %>
  <% if @invitation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invitation.errors.count, "error") %> prohibited this invitation from being saved:</h2>

      <ul>
      <% @invitation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= link_to_box "Invite from Yahoo", contacts_url %>

  <div class="field">
    <%= f.label :subjects %><br />
    <%= f.text_field :subjects %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :contacts %><br />
    <%= f.text_field :contacts %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
